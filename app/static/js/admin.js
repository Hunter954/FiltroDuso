(()=>{const el=document.getElementById('chart');if(!el)return;const data=JSON.parse(el.dataset.chart||'[]');const max=Math.max(1,...data.flatMap(x=>[x.views,x.completed]));el.innerHTML=data.map(x=>`<div class="chart-day"><div class="chart-bars"><i title="${x.views} visitas" style="height:${Math.max(1,x.views/max*100)}%"></i><i title="${x.completed} finalizadas" style="height:${Math.max(1,x.completed/max*100)}%"></i></div><small>${x.label}</small></div>`).join('')})();
